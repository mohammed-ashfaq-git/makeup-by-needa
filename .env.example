# Local development environment (git-ignored). Copy values into your host's env settings in production.

# MySQL connection string used by Drizzle ORM.
# Local dev example (see scripts/dev-db.sh to boot a local MySQL):
DATABASE_URL="mysql://mbn_app:dev-mbn-3f9a2c@127.0.0.1:3306/makeup_by_needa"

# Optional: when set, creating the first administrator at /admin/setup additionally
# requires this secret to be entered. It is only ever compared on the server —
# it is never sent to the browser.
# ADMIN_SETUP_SECRET="change-me"
